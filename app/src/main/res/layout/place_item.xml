<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/placeMainLayout">

    <LinearLayout style="@style/placeItem">

        <ImageView
            android:id="@+id/placeImage"
            android:src="@drawable/arc_de_triomf"
            style="@style/placeImage" />

        <LinearLayout style="@style/placeContentLayout">

            <RelativeLayout style="@style/titleLayout">

                <TextView
                    android:id="@+id/placeName"
                    tools:text="Arc de Triomf"
                    style="@style/paceName" />

                <ImageView
                    android:id="@+id/pinIcon"
                    android:src="@drawable/ic_navigation_black_24dp"
                    style="@style/pinIcon" />
            </RelativeLayout>

            <TextView
                android:id="@+id/placeShortDescription"
                tools:text="The Arc de Triomf was built as the gateway to the fair which was held in the Parc de la Ciutadella. The monument is classical in shape and proportions and features ground-breaking sculptural and decorative finishes replete with symbolism. It has become one of the city's iconic landmarks."
                style="@style/placeDescription" />
            <TextView
                android:id="@+id/address"
                tools:text="Address: Plaza España, 08018 Barcelona"
                style="@style/placeAddress" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
